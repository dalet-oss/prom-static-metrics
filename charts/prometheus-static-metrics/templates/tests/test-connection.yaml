apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "prometheus-static-metrics.fullname" . }}-test-connection"
  labels:
    {{- include "prometheus-static-metrics.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "prometheus-static-metrics.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
