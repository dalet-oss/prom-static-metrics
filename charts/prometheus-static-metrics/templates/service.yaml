apiVersion: v1
kind: Service
metadata:
  name: {{ include "prometheus-static-metrics.fullname" . }}
  labels:
    {{- include "prometheus-static-metrics.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: metrics
      protocol: TCP
      name: metrics
  selector:
    {{- include "prometheus-static-metrics.selectorLabels" . | nindent 4 }}
